# Node group 🗄
Search which mutants can be tested simultaneously.

## Installation
```sh
    cp src/settings-copy.ts src/settings.ts
    npm i
```

## Run
Change settings as desired and run ```npm run watch``` and ```npm start```.

## Disclaimer
The package filing-cabinet has got an issue so not every project will have the expected result.